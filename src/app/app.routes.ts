import { Routes } from '@angular/router';
import { SalesPrediction } from './components/sales-prediction/sales-prediction';

export const routes: Routes = [
     { path: '', component: SalesPrediction },     
];
